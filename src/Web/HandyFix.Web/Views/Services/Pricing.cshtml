@model IEnumerable<HandyFix.Web.ViewModels.Services.CategoryViewModel>

<div class="page-container services-page">
    <!-- Header & Breadcrumbs -->
    <section class="services-header">
        <nav class="breadcrumb-nav" aria-label="breadcrumb">
            <a asp-controller="Home" asp-action="Index">Home</a>
            <span class="material-symbols-outlined breadcrumb-separator">chevron_right</span>
            <span class="breadcrumb-current">Pricing</span>
        </nav>
        <div class="services-header-content">
            <h1 class="services-hero-title">
                Transparent, Up-Front <span>Pricing.</span>
            </h1>
            <p class="services-subtitle">
                No hidden fees or call-out charges. Every job starts with a 1-hour base rate for the service category, billed at the hourly rate shown below.
            </p>
        </div>
    </section>

    <!-- Pricing Tables by Category -->
    <section class="pricing-categories">
        @foreach (var category in this.Model)
        {
            <div class="pricing-category-section">
                <div class="pricing-category-header">
                    <div>
                        <h2 class="pricing-category-title">@category.Name</h2>
                        <p class="pricing-category-desc">@category.Description</p>
                    </div>
                </div>

                <div class="pricing-table-wrapper">
                    <table class="pricing-table">
                        <thead>
                            <tr>
                                <th>Service</th>
                                <th>Estimated Duration</th>
                                <th>Hourly Rate</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var svc in category.Services)
                            {
                                var duration = svc.EstimatedDurationMinutes >= 60
                                    ? $"{(svc.EstimatedDurationMinutes / 60.0).ToString("0.#")} hours"
                                    : $"{svc.EstimatedDurationMinutes} mins";

                                <tr>
                                    <td class="pricing-service-name">
                                        <a asp-route="ServiceDetails" asp-route-categorySlug="@category.Slug" asp-route-serviceSlug="@svc.Slug">@svc.Name</a>
                                    </td>
                                    <td>@duration</td>
                                    <td class="pricing-price">From £@svc.BasePrice.ToString("F2")/hr</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }
    </section>

    <!-- Pricing Terms Note -->
    <div class="pricing-note-box">
        <h3>How our pricing works</h3>
        <p>
            All bookings begin with a 1-hour base rate for the selected service category. If a job takes longer, additional
            time is billed at the same hourly rate. Any materials required beyond what our technician carries as standard
            are quoted and agreed with you before work begins &mdash; no surprises on the invoice.
        </p>
    </div>

    <!-- CTA -->
    <div class="pricing-cta-actions">
        <a asp-controller="Booking" asp-action="Index" class="btn btn-primary btn-lg">Book Appointment Now</a>
        <a asp-controller="Home" asp-action="Contact" class="btn btn-outline-primary btn-lg">Get a Custom Quote</a>
    </div>
</div>
